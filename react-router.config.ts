import type { Config } from "@react-router/dev/config";

export default {
  // Config options will be added here based on your needs
  ssr: true,
  buildDirectory: "dist",
  appDirectory: "src/app",
} satisfies Config;
